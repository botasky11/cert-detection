# 基于 CMU-CERT r4.2 的行为序列匹配恶意行为识别项目研究报告

报告日期：2026-05-07

## 摘要

内部威胁检测面临样本稀缺、行为跨度长、攻击链隐蔽和误报成本高等问题。与传统二分类建模相比，本项目采用行为序列匹配思路，将 CMU-CERT r4.2 数据集中分散在登录、设备、文件、HTTP 与邮件等多源日志中的原始事件统一抽象为行为 token 时间序列，并围绕红队场景构建可解释的序列模式检测器。

系统核心由四类互补检测器组成：红队剧本模板的有序子序列匹配、良性用户 N-gram 语言模型异常度、与恶意签名序列的 LCS 相似度，以及高风险 token 的群体稀有度偏离。最终通过归一化加权融合得到用户级风险分数，用于对 1000 名员工进行风险排序。当前实验结果显示，在官方 r4.2 ground truth 的 70 名恶意用户设定下，全量用户 ROC-AUC 为 0.9051，PR-AUC 为 0.3278；在留出评估集上 ROC-AUC 为 0.9052，PR-AUC 为 0.6066，Top-100 Recall 达到 0.8143。结果表明，基于行为序列的规则增强方法能够在弱监督场景下实现较高召回，并保留较好的解释性。

## 1. 研究背景与意义

企业内部威胁通常并不表现为单个孤立事件，而是由一组在时间上相互关联的行为组成，例如非工作时间登录、连接移动存储设备、访问泄露站点、拷贝敏感文件或向外部邮箱发送附件。单点异常检测容易受到办公噪声干扰，而纯监督学习又依赖大量高质量标签，在真实环境中难以满足。

CMU-CERT r4.2 是内部威胁检测研究中常用的模拟数据集，包含多个月份、多源日志与红队注入的恶意用户行为。该数据集适合研究以下问题：

- 如何将异构日志转换为统一的用户行为序列；
- 如何从攻击剧本中抽取有序行为模式；
- 如何在标签稀缺条件下利用良性群体行为建立异常基线；
- 如何在检测结果中保留可解释性，辅助安全分析人员排查。

本项目围绕“行为序列匹配”开展研究，目标不是单纯追求黑盒分类精度，而是构建一套可复现、可解释、可扩展的内部威胁识别流程。

## 2. 数据集与威胁场景

项目基于 CMU-CERT r4.2 数据集。原始数据约 16 GB，覆盖 1000 名员工、约 17 个月的企业活动日志，主要包括：

| 数据源 | 主要字段 | 行为含义 |
| --- | --- | --- |
| logon.csv | date, user, pc, activity | 登录/登出行为，识别工作时间、非工作时间和是否使用本人常用 PC |
| device.csv | date, user, pc, activity | USB 设备连接/断开行为 |
| file.csv | date, user, pc, filename | 文件访问行为，按文档、可执行文件和其他文件分类 |
| http_slim.csv | date, user, pc, url | 访问敏感网站行为，如 wikileaks、keylogger、求职网站 |
| email.csv | date, user, recipients, attachments | 内外部邮件、附件外发行为 |
| LDAP | user, role, department, team | 用户岗位与组织属性 |

当前项目使用 `src/build_ground_truth.py` 中经官方 `insiders.csv` 核对的 r4.2 恶意用户清单，共 70 个唯一恶意用户，分为三类典型场景：

| 场景 | 用户数 | 行为剧本 |
| --- | ---: | --- |
| S1 数据泄露/USB 外带 | 30 | 离职或不满员工在非工作时段登录、连接 U 盘、访问或拷贝文档，并上传至泄露站点 |
| S2 跳槽前数据外发 | 30 | 员工浏览求职网站，窃取竞品或业务资料，并通过个人邮箱或外部邮件渠道外发 |
| S3 系统管理员键盘记录器 | 10 | 管理员访问 keylogger 站点、安装可执行文件，并冒用高管身份发送破坏性邮件 |

说明：仓库 README 中部分早期描述仍保留“60 个恶意用户/51 个有事件”的口径，但当前源码与 `outputs/metrics.json` 均以 70 个官方 r4.2 恶意用户为准。本报告采用当前实现与输出结果。

## 3. 数据预处理与行为 token 化

预处理脚本为 `src/preprocess.py`。其核心思想是将多源日志流式读取后统一为按用户、按时间排序的行为 token 序列，输出到 `outputs/user_sequences.pkl` 和 `outputs/user_daily_seq.pkl`。

### 3.1 时间上下文

项目将时间划分为工作时间与非工作时间：

- 工作时间 WH：周一至周五 07:00-18:00；
- 非工作时间 AH：周末、深夜、清晨以及其他非工作时间段。

时间上下文对内部威胁检测非常关键。同样是登录、拷贝文件或连接 U 盘，发生在非工作时间通常比工作时间更可疑。

### 3.2 用户上下文

项目从登录日志中统计每个用户最常使用的 PC，作为该用户的“常用 PC”。登录事件随后被区分为：

- `LOGON_WH_OWN`：工作时间登录本人常用 PC；
- `LOGON_AH_OWN`：非工作时间登录本人常用 PC；
- `LOGON_AH_OTHER`：非工作时间登录他人或非常用 PC。

这种上下文增强可以捕捉横向移动、冒用设备或非典型办公位置等异常行为。

### 3.3 行为 token 设计

项目最终形成约 30 类行为 token，典型示例如下：

| 行为类别 | token 示例 | 含义 |
| --- | --- | --- |
| 登录 | `LOGON_AH_OTHER` | 非工作时间登录非常用 PC |
| 设备 | `USB_CONN_AH` | 非工作时间连接 USB 设备 |
| 文件 | `FILE_DOC_AH` | 非工作时间访问文档类文件 |
| 文件 | `FILE_EXEC_WH` | 工作时间访问或执行可执行类文件 |
| HTTP | `HTTP_LEAK_AH` | 非工作时间访问泄露相关站点 |
| HTTP | `HTTP_KEYLOG_WH` | 工作时间访问 keylogger 相关站点 |
| HTTP | `HTTP_JOBHUNT_WH` | 工作时间访问求职网站 |
| 邮件 | `EMAIL_EXT_ATT_WH` | 工作时间向外部邮箱发送附件邮件 |

HTTP 日志体量较大，因此项目仅保留命中敏感关键词的 URL 访问行为，以降低存储与计算成本。邮件日志则重点关注外部收件人与附件数量，识别外发和附件外发行为。

## 4. 方法设计

项目核心模型定义在 `src/sequence_matching.py` 中。整体架构如下：

```text
多源日志
  -> 行为 token 序列
  -> PatternRule / NGramAnomaly / SequenceSimilarity / PeerDeviation
  -> 分量归一化
  -> 用户风险分数 R(u)
  -> 排名、Top-K 指标与解释
```

### 4.1 威胁权重设计

项目基于 token 在恶意用户和良性用户中的出现比例差异，为不同 token 设置威胁权重。高区分度行为获得更高权重，常见办公行为权重较低或置零。

典型权重如下：

| token | 权重 | 解释 |
| --- | ---: | --- |
| `HTTP_LEAK_AH` / `HTTP_LEAK_WH` | 8.0 | 访问泄露站点，强恶意信号 |
| `HTTP_KEYLOG_AH` / `HTTP_KEYLOG_WH` | 8.0 | 访问键盘记录器相关站点，强恶意信号 |
| `USB_CONN_AH` | 2.5 | 非工作时间连接移动设备，较强信号 |
| `FILE_EXEC_AH` | 2.0 | 非工作时间执行或访问可执行文件，较强信号 |
| `LOGON_AH_OTHER` | 2.0 | 非工作时间登录非常用 PC，中高风险 |
| `HTTP_JOBHUNT_WH` | 0.0 | 求职网站在良性用户中也很常见，单独出现区分度低 |

这种设计避免了模型被高频但低风险行为误导，同时强化了少数真正具有判别力的攻击链节点。

### 4.2 PatternRule：红队剧本有序子序列匹配

PatternRule 检测器将三类红队场景编码为行为模板，例如 S1 包含“非工作时间登录、连接 U 盘、访问文档或泄露站点”等组合，S3 包含“访问 keylogger、执行文件、外发邮件”等组合。

模型并不要求 token 严格连续出现，而是在窗口内进行有序子序列匹配，默认窗口大小为 15，并采用不重叠计数。模板得分为：

```text
score(pattern) = log(1 + hits) * len(pattern)^1.5 * (1 + sum(threat_weight(token)))
```

该设计兼顾两点：攻击链可能被正常行为插入打断，因此需要允许间隔；重复命中应增加风险，但不能线性无限放大。

### 4.3 NGramAnomaly：良性行为语言模型

NGramAnomaly 检测器使用 70% 良性用户训练 3-gram 语言模型，并通过加性平滑计算被测用户序列的 perplexity。若某用户的行为序列在良性群体语言模型下概率较低，则 perplexity 较高，表示行为模式偏离正常办公序列。

当前实现中每个训练用户最多采样最近 2000 个 token，测试时最多使用最近 5000 个 token，以控制计算成本并强调近期行为。

### 4.4 SequenceSimilarity：恶意签名相似度

SequenceSimilarity 检测器将每个场景的模板 token 拼接成恶意签名序列，并计算用户行为序列与签名序列的最长公共子序列比例。同时，对高敏感 token 的出现次数进行加权统计。

得分形式为：

```text
score = 0.4 * LCS_ratio + 0.6 * weighted_high_sensitive_hit
```

该检测器能够捕捉“整体行为轮廓”与恶意剧本的相似程度，弥补单个规则模板覆盖不足的问题。

### 4.5 PeerDeviation：高风险 token 群体稀有度

PeerDeviation 只关注威胁权重大于等于 1.5 的 token，并在良性训练用户上计算 IDF：

```text
score = sum(log(1 + count_u(token)) * IDF(token) * threat_weight(token))
IDF(token) = log((N + 1) / (df(token) + 1))
```

该模块的作用是识别“别人很少有、而某用户反复出现”的高风险行为。它避免普通办公高频 token 造成长序列用户的误报。

### 4.6 融合风险分数

四个检测器输出后，项目对每个分量进行 min-max 归一化，并线性融合为用户风险分数：

```text
R(u) = 0.40 * z(rule)
     + 0.20 * z(perplexity)
     + 0.25 * z(similarity)
     + 0.15 * z(peer)
```

说明：`README.md` 中仍保留早期权重 `0.30/0.10/0.30/0.30` 的描述；当前 `src/train_evaluate.py` 实际运行使用的是 `0.40/0.20/0.25/0.15`。本报告以当前代码为准。

## 5. 实验设计

实验流程定义在 `src/train_evaluate.py` 中：

1. 加载预处理后的用户行为序列；
2. 从 `src/build_ground_truth.py` 加载 70 名恶意用户标签；
3. 将良性用户随机划分为 70% 训练用户和 30% 留出评估用户；
4. 使用良性训练用户拟合 N-gram 语言模型与 PeerDeviation 的 IDF 统计；
5. 对全部 1000 名用户打分并排序；
6. 在全量用户与评估集上分别计算 ROC-AUC、PR-AUC、Top-K Precision 与 Top-K Recall；
7. 输出风险分数、恶意用户排名和 Top-80 用户解释。

评估输出包括：

| 文件 | 内容 |
| --- | --- |
| `outputs/risk_scores.csv` | 1000 名用户的融合风险分数和各分量得分 |
| `outputs/malicious_user_ranking.csv` | 70 名恶意用户在全量排序中的名次 |
| `outputs/top_risk_explanations.json` | Top-80 高风险用户的规则命中和相似度解释 |
| `outputs/metrics.json` | 全量集与评估集指标 |
| `outputs/fig_*.png` | ROC/PR、Top-K、风险分布和场景覆盖等可视化 |

## 6. 实验结果

### 6.1 总体指标

当前 `outputs/metrics.json` 中的评估结果如下：

| 指标 | 全量 1000 用户 | 留出评估集 349 用户 |
| --- | ---: | ---: |
| 恶意用户数 | 70 | 70 |
| 良性用户数 | 930 | 279 |
| ROC-AUC | 0.9051 | 0.9052 |
| PR-AUC | 0.3278 | 0.6066 |
| Top-20 Precision | 0.3000 | 0.5000 |
| Top-40 Precision | 0.2750 | 0.6000 |
| Top-60 Precision | 0.2667 | 0.6500 |
| Top-100 Precision | 0.3700 | 0.5700 |
| Top-100 Recall | 0.5286 | 0.8143 |

ROC-AUC 在全量集和评估集上均约为 0.905，说明融合分数对恶意用户与良性用户具有较稳定的排序区分能力。PR-AUC 在评估集上显著高于全量集，主要因为评估集中恶意样本占比更高；在全量 1000 用户中，恶意用户仅占 7%，PR-AUC 更能反映实际排查场景中的难度。

![ROC 与 PR 曲线](outputs/fig_roc_pr.png)

### 6.2 Top-K 风险排查效果

在安全运营场景中，分析人员通常优先查看风险排名靠前的用户，因此 Top-K 指标比单纯分类阈值更有实际意义。

| K | 命中恶意用户数 | 全量召回率 | 场景分布 |
| ---: | ---: | ---: | --- |
| 20 | 6 | 0.0857 | S1: 1, S2: 1, S3: 4 |
| 40 | 11 | 0.1571 | S1: 6, S2: 1, S3: 4 |
| 60 | 16 | 0.2286 | S1: 10, S2: 2, S3: 4 |
| 80 | 24 | 0.3429 | S1: 15, S2: 3, S3: 6 |
| 100 | 37 | 0.5286 | S1: 26, S2: 5, S3: 6 |
| 150 | 49 | 0.7000 | S1: 30, S2: 10, S3: 9 |
| 200 | 58 | 0.8286 | S1: 30, S2: 19, S3: 9 |
| 300 | 68 | 0.9714 | S1: 30, S2: 28, S3: 10 |

结果显示，模型对 S1 和 S3 场景较敏感，尤其是访问泄露站点、连接 U 盘、访问 keylogger 和执行文件等行为具有较高区分度。S2 场景由于求职网站访问和工作时间文档行为在良性用户中也较常见，整体排名更分散，是当前模型的主要改进空间。

![Top-K Precision 与 Recall](outputs/fig_topk.png)

### 6.3 风险分数分布

全量用户中，良性用户平均风险分数为 0.1176，中位数为 0.0870；恶意用户平均风险分数为 0.3304，中位数为 0.3250。两类用户的分布存在明显分离，但仍有少数良性用户风险分数较高，最高良性分数达到 0.7927。这说明部分良性用户在 token 层面与红队剧本存在较强重合，可能代表误报，也可能反映了数据集中正常办公行为与模拟攻击行为之间的边界模糊。

![风险分数分布](outputs/fig_risk_distribution.png)

### 6.4 场景检测差异

按恶意场景统计，当前模型表现如下：

| 场景 | 用户数 | 平均风险分数 | 中位风险分数 | 最好排名 | 最差排名 |
| --- | ---: | ---: | ---: | ---: | ---: |
| S1 | 30 | 0.3753 | 0.3579 | 15 | 135 |
| S2 | 30 | 0.2355 | 0.2171 | 10 | 408 |
| S3 | 10 | 0.4805 | 0.3768 | 1 | 271 |

S3 的平均风险最高，因为 keylogger 相关 token 稀有且威胁权重高；S1 的稳定性较好，全部 30 个 S1 用户均进入 Top-150；S2 的排名波动最大，最差排名为 408，说明“求职 + 文档 + 外发”的组合仍不足以稳定地区分恶意跳槽行为与正常业务行为。

![场景覆盖](outputs/fig_scenario_breakdown.png)

### 6.5 高风险用户解释

模型输出不仅包含最终风险分数，还保留了各检测器分量与规则命中情况。Top 风险用户示例如下：

| 排名 | 用户 | 风险分数 | 真实场景 | 主要特征 |
| ---: | --- | ---: | ---: | --- |
| 1 | CCA0046 | 0.8517 | S3 | PatternRule、SequenceSimilarity 和 PeerDeviation 均显著偏高 |
| 3 | MPM0220 | 0.7906 | S3 | keylogger 相关签名和稀有行为强命中 |
| 6 | BSS0369 | 0.7487 | S3 | 与 S3 签名序列相似度最高 |
| 7 | GTD0219 | 0.7467 | S3 | 高敏感 token 命中和 peer 偏离明显 |
| 10 | MOS0047 | 0.6889 | S2 | 在 S2 场景下出现较高规则和相似度得分 |
| 15 | MCF0600 | 0.5670 | S1 | USB、文件和泄露相关行为组合明显 |

![Top-30 检测器贡献](outputs/fig_top30_contribution.png)

## 7. 可解释性分析

与端到端黑盒分类器相比，本项目的主要优点是每个高风险用户都可以追溯到具体检测器与具体场景规则：

- PatternRule 可以回答“用户命中了哪条红队行为模板”；
- SequenceSimilarity 可以回答“用户整体序列更像哪类恶意签名”；
- PeerDeviation 可以回答“用户有哪些群体中罕见的高风险行为”；
- NGramAnomaly 可以回答“用户行为在良性语言模型下是否反常”。

这种解释结构适合 SOC 或内部审计场景。分析人员可以先查看 Top-K 排名，再根据 `top_risk_explanations.json` 中的场景命中情况定位具体行为链，而不是只得到一个难以解释的分类结果。

## 8. 局限性

当前项目仍存在以下限制：

- Ground truth 依赖官方红队清单，尚未处理真实业务环境中的未知攻击类型；
- 行为 token 粒度较粗，文件路径、邮件正文、收件人关系和岗位权限等信息尚未充分利用；
- S2 场景中的求职访问与文档行为在良性用户中较常见，导致该场景召回排序偏后；
- 当前融合权重为人工设定，尚未通过交叉验证或贝叶斯优化系统调参；
- N-gram 语言模型只使用群体良性基线，尚未建立用户个人历史基线；
- 预处理路径目前硬编码为 `/home/user/webapp/...`，跨环境复现需要修改路径或参数化配置；
- README 与当前源码/指标存在口径差异，需要同步 70 个恶意用户和当前融合权重。

## 9. 改进方向

后续可以从以下方向扩展：

1. 引入个性化基线：以用户自身过去 30 天或 60 天行为作为参照，检测行为漂移，降低高活跃良性用户误报。
2. 自动挖掘序列模式：使用 PrefixSpan、GSP 或 SPADE 在恶意与良性子集中挖掘差异化频繁序列，补充人工模板。
3. 加强 S2 场景建模：结合邮件收件人图、附件大小、文件主题、离职时间窗口和岗位变化信息，提升跳槽外发行为识别能力。
4. 学习融合权重：在保持可解释性的前提下，用逻辑回归、排序学习或校准模型学习四个分量权重。
5. 引入图结构特征：构建用户-设备、用户-文件、用户-收件人图，识别跨设备访问、异常外联和组织关系偏离。
6. 增加阈值策略：根据 SOC 日处理能力设置 Top-K 或动态阈值，并输出可操作的告警等级。
7. 工程化配置：将数据路径、时间窗口、权重、关键词列表和输出目录配置化，便于复现实验和迁移部署。

## 10. 结论

本项目实现了一套面向 CMU-CERT r4.2 内部威胁数据集的行为序列匹配识别系统。系统将多源日志抽象为统一的行为 token 序列，并融合规则模板、语言模型异常、恶意签名相似度和群体稀有度四类检测信号，在 1000 名用户的排序任务上取得了 0.9051 的全量 ROC-AUC 和 0.5286 的 Top-100 Recall。

实验结果说明，内部威胁检测不应只依赖单点异常，而应关注跨时间、跨数据源的行为链。当前方法在 S1 数据泄露和 S3 keylogger 场景上表现较好，在 S2 跳槽外发场景上仍有提升空间。整体而言，该项目具备较好的可解释性、复现性和工程扩展基础，适合作为内部威胁序列检测研究的可运行原型。

## 附录：项目文件对应关系

| 文件 | 作用 |
| --- | --- |
| `src/preprocess.py` | 多源日志流式读取、行为 token 化、用户序列构建 |
| `src/build_ground_truth.py` | CMU-CERT r4.2 官方恶意用户清单 |
| `src/sequence_matching.py` | 四个检测器与风险融合模型 |
| `src/train_evaluate.py` | 训练、打分、评估与解释输出 |
| `src/visualize.py` | 评估图表生成 |
| `outputs/metrics.json` | 当前实验指标 |
| `outputs/risk_scores.csv` | 用户风险分数与分量得分 |
| `outputs/malicious_user_ranking.csv` | 恶意用户排名 |
| `outputs/top_risk_explanations.json` | Top 风险用户解释 |
